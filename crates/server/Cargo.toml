[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = { version = "0.8.9", features = ["ws"] }
common = { version = "0.1.0", path = "../common" }
tokio = { version = "1.53.1", features = ["full"] }
